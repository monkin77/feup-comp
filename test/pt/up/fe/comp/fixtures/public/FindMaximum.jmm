import io;

class FindMaximum {
    int _x;

    public int f() {
        int x;
        _x = 3;
        x = _x;
        return x;
    }

    // TODO: Implement ollir constructor calls
    //public FindMaximum fm() {
    //    FindMaximum fm;
    //    return fm;
    //}

    public static void main(String[] args) {
        int x;
        int y;
        int[] a;
        boolean b;
        FindMaximum fm1;
        x = 0;
        x = 1 + 2;
        io.println(x);
        x = args.length;
        b = false;
        x = 1 + 2 - 3 * 4 / 5;
        b = x < 1;
        b = b && b;
        b = !b;
        a[0] = 1;
        if (x < 3) {
            x = 3;
        } else {
            x = x;
        }
        io.println(x);
        while (x < 10) {
           x = x + 1;
           io.println(x);
        }
        // a = new int[15]; // TODO: needs to be reviewed in Jasmin, but wtf?
        fm1._x = 10; // TODO: What?
        // fm1.fm().fm().fm().fm();
    }
}