import MathUtils;
import io;

class FindMaximum {
    int _x;

    public int f() {
        int x;
        _x = 3;
        x = _x;
        return 8;
    }

    public FindMaximum fm() {
        FindMaximum fm2;
        fm2 = new FindMaximum();
        io.println(69420);
        return fm2;
    }

    public static void main(String[] args) {
        int x;
        int y;
        int[] a;
        boolean b;
        FindMaximum fm1;
        x = 0;
        x = 1 + 2;
        io.println(x);
        x = args.length;
        b = false;
        x = 1 + 2 - 3 * 4 / 5;
        b = x < 1;
        b = b && b;
        b = !b;
        if (x < 3) {
            x = 3;
        } else {
            x = x;
        }
        io.println(x);
        while (x < 10) {
            x = x + 1;
            io.println(x);
        }
        a = new int[15];
        x = MathUtils.random(3, 3);
        fm1 = new FindMaximum();
        fm1.fm().fm().fm().fm();
        io.println(a.length);
        x = a.length;
        while (0 < x) {
            a[x - 1] = x * x;
            x = x - 1;
        }
        y = x + a[0];
        x = a.length;
        while (0 < x) {
            y = a[x - 1];
            io.println(y);
            x = x - 1;
        }
        io.println(a[0 + 3]);
    }
}