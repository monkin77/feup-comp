import MathUtils;
import io;

class FindMaximum {
    int _x;
    int[] fieldArray;
    FindMaximum fm2;

    public int f() {
        int x;
        FindMaximum fm1;
        _x = 3;
        x = _x;
        fieldArray = new int[10];
        x = fieldArray.length;
        fieldArray[0] = 5;
        x = fieldArray[0];
        fieldArray[5] = 3;
        fieldArray[3] = 1;
        fieldArray[fieldArray[0 * 5]] = fieldArray[fieldArray[fieldArray[0]]];
        fieldArray[fieldArray.length - 2] = fieldArray[0];
        fm1 = new FindMaximum();
        fm1.fm();
        return 8;
    }

    public FindMaximum fm() {
        FindMaximum fm2;
        fm2 = new FindMaximum();
        io.println(69420);
        return fm2;
    }

    public static void main(String[] args) {
        int x;
        int y;
        int[] a;
        boolean b;
        FindMaximum fm1;
        x = 0;
        x = 1 + 2;
        io.println(x);
        x = args.length;
        b = false;
        x = 1 + 2 - 3 * 4 / 5;
        b = x < 1;
        b = b && b;
        b = !b;
        if (x < 3) {
            x = 3;
        } else {
            x = x;
        }
        io.println(x);
        while (x < 10) {
            x = x + 1;
            io.println(x);
        }
        a = new int[15];
        x = MathUtils.random(3, 3);
        fm1 = new FindMaximum();
        fm1.fm().fm().fm().fm();
        io.println(a.length);
        x = a.length;
        while (0 < x) {
            a[x - 1] = x * x;
            x = x - 1;
        }
        y = x + a[0];
        x = a.length;
        while (0 < x) {
            y = a[x - 1];
            io.println(y);
            x = x - 1;
        }
        io.println(a[0 + 3]);
        io.println(new FindMaximum().fm().f());
        io.println(new int[250].length);
    }
}